@Library('sap-test-library') _

pipeline{
    agent any
    stages{
        stage("TEST LIB"){
            steps{
                script{
                    echo "jenkins home : $JENKINS_HOME"
                    echo "ORKSPACE : $WORKSPACE"
                }
            }
        }
    }
}
